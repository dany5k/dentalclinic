import { CustomtwoDirective } from './customtwo.directive';

describe('CustomtwoDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomtwoDirective();
    expect(directive).toBeTruthy();
  });
});
