import { CustomoneDirective } from './customone.directive';

describe('CustomoneDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomoneDirective();
    expect(directive).toBeTruthy();
  });
});
